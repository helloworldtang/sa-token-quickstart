server:
  port: 8082

spring:
  application:
    name: order-service
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# Sa-Token 配置
sa-token:
  token-name: satoken
  timeout: 2592000
  active-timeout: 1800

# 日志配置
logging:
  level:
    root: INFO
    com.tangtang: DEBUG
